# NEVER commit a real token. Copy to .env locally (gitignored).
# Optional: raises GitHub API rate limit for local builds (homepage repo grid).
# CI uses the ephemeral GITHUB_TOKEN secret; do not paste that here.
GITHUB_TOKEN=
